package service;

import java.nio.file.Path;
import java.nio.file.Paths;

public class ReadFileService {
    public void readShoppingList(String path) {
        Path filePath = Paths.get(path);


    }
}
